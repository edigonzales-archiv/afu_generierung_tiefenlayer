CREATE TABLE IF NOT EXISTS 
  ${schema_name}.stupid_result
  (
    rid int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, 
    rast raster
  )
WITH (
	OIDS=FALSE
);


CREATE INDEX ${schema_name}_stupid_result_rast_gist_idx ON ${schema_name}.stupid_result USING GIST (ST_ConvexHull(rast));

